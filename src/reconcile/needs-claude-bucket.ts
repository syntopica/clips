/** The bucket and status name a clip lands in when it needs a human. One
 * constant standing for the literal used across the move, the mirror, and
 * the ledger's own `status` field, all of which mean the same thing. */
export const NEEDS_CLAUDE_BUCKET = 'needs-claude'
