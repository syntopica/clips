/** Known metadata.json schema versions, accepted explicitly. Anything else is
 * UNSUPPORTED_CLIP_SCHEMA. Version 2 is the mobile Shortcut's shape and is
 * added in plan 2d, together with the normalisation that makes it readable. */
export const SUPPORTED_SCHEMA_VERSIONS: readonly number[] = [1]
