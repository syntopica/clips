/** A topic names a `<topic>.md` file, so it is held to the same slug shape
 * the configuration schema enforces on `newsletter.triageTopics`. */
export const TRIAGE_TOPIC_PATTERN = /^[a-z0-9]+(?:-[a-z0-9]+)*$/
