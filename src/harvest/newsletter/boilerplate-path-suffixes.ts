/** Navigation paths repeated in every digest footer. Suffixes rather than exact
 * paths, because Medium serves the same pages under a publication prefix. */
export const BOILERPLATE_PATH_SUFFIXES: readonly string[] = [
  '/m/signin',
  '/plans',
  '/membership',
  '/about',
  '/blog',
]
