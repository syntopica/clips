#!/usr/bin/env node
import { realpathSync } from 'node:fs'
import { dirname, resolve } from 'node:path'
import { pathToFileURL } from 'node:url'

await import(
  pathToFileURL(
    resolve(dirname(realpathSync(process.argv[1])), '../src/main.ts'),
  ).href
)
